export const metadata = {
  title: "Sign Up",
  description: "Create a Hireloop account to find your next job or hire top-tier talent.",
};

export default function SignupLayout({ children }) {
  return children;
}
