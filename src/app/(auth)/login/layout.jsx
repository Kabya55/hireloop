export const metadata = {
  title: "Login",
  description: "Log in to your Hireloop account to manage jobs, apply for positions, or hire talent.",
};

export default function LoginLayout({ children }) {
  return children;
}
